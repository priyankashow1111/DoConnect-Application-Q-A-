spring.application.name=user-service

# Port
server.port=8081

# MySQL Database
spring.datasource.url=jdbc:mysql://localhost:3306/doconnect_user_db
spring.datasource.username=root
spring.datasource.password=Priyanka@1111
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# Eureka
eureka.client.service-url.defaultZone=http://localhost:8761/eureka/
eureka.instance.prefer-ip-address=true

# Notification service URL
notification.service.url=http://localhost:8083

# JWT Secret Key (keep this secret)
jwt.secret=doconnect_super_secret_key_123456789_abcdefgh
jwt.expiration=86400000